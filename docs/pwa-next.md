# PWA Next Step

The web app already includes a manifest and mobile operation screen.

Remaining PWA items:

- Add service worker file in public folder
- Register service worker from main entry
- Add app icons
- Add barcode scanner page
- Add offline queue for stock actions
- Add install instructions for iPhone and Android

Keep this step after build verification so the app does not break before release.
